name: Build S901E NetHunter Kernel

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Smoke test
        run: |
          echo "workflow parsed OK"
          pwd
          ls -la
